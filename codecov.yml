codecov:
  require_ci_to_pass: true
  notify:
    wait_for_ci: true

coverage:
  status:
    project:
      default:
        enabled: true
        target: 80%
        threshold: 5%
    patch:
      enabled: false
