# `eslint-config`

ESLint configuration for AutoPalette.

## Installation

```sh
pnpm add @auto-palette/eslint-config --save-dev --workspace
```

## Usage

```json
{
  "extends": ["@auto-palette/eslint-config"]
}
```
